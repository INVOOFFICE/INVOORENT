/**
 * Démarrage (APP-GUIDE) : preloadOPFS → checkAuth → shell UI dans le DOMContentLoaded de 01-app-core.js
 * (fonction bootstrapAfterAuth). Ce fichier documente l’ordre ; ne pas dupliquer le bootstrap ici.
 */
globalThis.__INVOORENT_APP_GUIDE_BOOTSTRAP__=true;
